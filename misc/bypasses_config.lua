-- External config for bypasses_improved.lua
-- Edit this file to enable/disable specific bypasses without modifying the main script.

return {
    gc_spoof = true,
    gc_amplitude = 150,
    mem_spoof = true,
    mem_rand_range = 0.1,
    contentprovider_randomize = true,
    debug = false,
}
